<!DOCTYPE html>
<html>
	<head>
		<meta charset="UTF-8">
		<title>
			{% block title %}Welcome!
			{% endblock %}
		</title>

		<link href="http://localhost/lara/public/build/app.css" rel="stylesheet"/>

		<script src="http://localhost/lara/public/build/app.js" defer></script>

		{# <img src="{{asset('images/paveiksliukas.gif')}}">
						        <img src="{{path('get_image',{id:85547})}}"> #}

		<!-- include summernote css/js -->
		<link href="https://cdn.jsdelivr.net/npm/summernote@0.8.18/dist/summernote.min.css" rel="stylesheet"> {% block stylesheets %}{% endblock %}{% block javascripts %}{% endblock %}
		</head>
		<body>

		{% include 'menu.html.twig' %}
			<main class="py-4">
				{% if errors is defined and errors|length > 0 %}
					{% include 'msg/validation.html.twig' %}
				{% endif %}

				{% if success is defined and success|length > 0 %}
					{% include 'msg/success.html.twig' %}
				{% endif %}

				{% block body %}{% endblock %}
			</main>
		</body>
	</html>